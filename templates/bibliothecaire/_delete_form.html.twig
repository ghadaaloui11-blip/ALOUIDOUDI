<form method="post" action="{{ path('app_bibliothecaire_delete', {'id': bibliothecaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bibliothecaire.id) }}">
    <button class="btn">Delete</button>
</form>
